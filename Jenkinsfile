node {
     stage('Build') {
      // Run the maven build
      build job: 'jenkinsinteg', quietPeriod: 0
   }
      stage('Test') {
      // Run the maven build
      build job: 'jenkinsinteg', quietPeriod: 0
   }
      stage('Deploy') {
      // Run the maven build
      build job: 'jenkinsinteg', quietPeriod: 0
   }
   
   
}
